import { StyleSheet } from 'react-native';


const styles = StyleSheet.create({
    container: {
      flex: 1,
    },
    splashLogo: {
      width: '100%', 
      height: '100%'
    }
  });

module.exports = styles;
